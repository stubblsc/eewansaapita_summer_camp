<% if is_admin? %>
<div class="col-md-3 offset-1">
<h1>New yearly_info</h1>

<%= render 'form' %>

<%= link_to 'Back', yearly_infos_path, :class => "btn btn-sm btn-danger" %>
</div>
<% else %>
You don't have access to this page
<% end %>